# Used by `npm run dev:backend` (vite --mode backend).
# Points at Member 4's backend — or at `npm run mock:server`, which serves the
# same endpoints on port 8000 so you can test the real WebSocket path today.
VITE_WS_URL=ws://localhost:8000/ws/live
VITE_API_BASE=http://localhost:8000
